- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.gitconfig: gitconfig

# Requires dotbot-brew plugin
- install-brew: true
- brewfile:
    - Brewfile
